1. The original icon pack has around 60 audio applications that share the same icon (two eighth notes beamed together). 
If the user has installed some of these applications, they appear indistinguishable in an application launcher or an application menu etc.

Each application will have its own distinct icon.

2. The original icon pack has around 52 multimedia player applications that share the same icon (a piece of vertical filmstrip).
If the user has installed some of these applications, they appear indistinguishable in an application launcher or an application menu etc.

Each application will have its own distinct icon.
